import { Inter, Manrope } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })
export const manrope = Manrope({ weight: "700", subsets: ['latin'], variable: '--font-manrope', })
